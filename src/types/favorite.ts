export type FavoriteAttraction = {
  id: string;
  contentId?: string;
  name: string;
  city: string;
  address: string;
  category: string;
  image: string;
  description: string;
  addedAt: number;
};
